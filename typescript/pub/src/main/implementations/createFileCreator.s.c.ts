
import { A } from "../api.generated"

export const $$: A.createFileCreator = ($d, $se) => {
    return ($is, $c) => {
        $c(($, $c) => {
            $is(
                $,
                ($i) => {
                    $d.pipeFountainPen(
                        $i,
                        $c,
                    )
                },
            )
        })
    }
}

