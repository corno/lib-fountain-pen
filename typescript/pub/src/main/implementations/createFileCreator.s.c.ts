
import { A } from "../api.generated"

export const $$: A.createFileCreator = ($d) => {
    return ($is, $c) => {
        $c(($, $c) => {
            $is(
                $,
                ($i) => {
                    $d.pipeFountainPen(
                        $i,
                        $c,
                    )
                },
            )
        })
    }
}

