import * as pd from 'pareto-core-data'

import { algorithm, bldrRef, constructor, functionReference } from "lib-pareto-typescript-project/dist/submodules/project/shorthands"

import * as g_project from "lib-pareto-typescript-project/dist/submodules/project"

const d = pd.d

export const $: g_project.T.Project._ltype.library.bindings.O.api.root<pd.SourceLocation> = {
    'algorithms': d({
    }),
}